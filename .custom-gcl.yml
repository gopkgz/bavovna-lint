# Pin `version` to the latest stable golangci-lint v2 release at build time.
version: v2.12.2
name: custom-gcl
destination: ./build/bin
plugins:
  - module: github.com/gopkgz/bavovna-lint
    path: .
